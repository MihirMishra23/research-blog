# Worklog — 2026-08-30 17:42:16 PDT

## Session objective

Remove the numeric concept target from the Post-V1 taxonomy plan and make the graph respond to the current production node set.

## Completed

- Removed the per-area count column, total row, 19-node target, displacement rule, and exact-size handoff requirement from the taxonomy scope.
- Kept the currently selected concepts as an editorial backlog rather than a release quota or maximum.
- Updated Action Item 1.1 to state that production topic files determine the current graph membership.
- Updated the layout workstream to derive canvas dimensions, area spacing, and generated positions from the current production node set.
- Removed the requirement to hand-author coordinates for routine topic additions; new topics remain drafts until the generated layout can place the resulting graph clearly.
- Updated the scope verifier to reject numeric target language and require the no-cap, source-of-truth, and dynamic-layout rules.

## Design decisions

- There is no fixed minimum, target, or maximum number of concepts.
- Adding a concept does not require removing an existing concept.
- Published topic metadata is the canonical source for graph membership; drafts remain preview-only.
- Identical production node sets must produce deterministic layout output, while adding or removing nodes triggers a fresh layout calculation.
- Optional authored overrides and exceptional edge routes remain available for editorial intent, but normal growth cannot depend on manually positioning every node.
- This change revises the plan and layout contract. The generated scalable layout itself remains the implementation work in Post-V1 Workstream 2.

## Validation

- Prettier formatting and formatting verification passed.
- The scope verifier confirmed seven active categories, an open-ended production-derived node set, and no numeric cap.
- Astro reported zero errors, warnings, or hints across 57 files.
- All 16 Node tests passed.
- The production build generated 18 pages and the Pagefind index successfully.
